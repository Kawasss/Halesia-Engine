define pause pauseGame = ((pauseGame == 1) - 1) * -1; # this basically is a different way of writing pauseGame != pauseGame, this is used because the language doesn't support the operator != yet
define showfps showFPS = ((showFPS == 1) - 1) * -1;
define playframe playOneFrame = ((playOneFrame == 1) - 1) * -1;