define pause pauseGame = !pauseGame; # this basically is a different way of writing pauseGame = !pauseGame, this is used because the language doesn't support the operator != yet
define showfps showFPS = !showFPS;
define playframe playOneFrame = !playOneFrame;
define showmetadata showMetaData = !showMetaData;